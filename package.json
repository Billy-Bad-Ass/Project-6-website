{
  "name": "bba-network-hub",
  "version": "1.0.0",
  "private": true,
  "description": "The apex site for bbanetwork.org — the hub that points at each separate business.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/generate-icons.mjs",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@fortawesome/fontawesome-free": "^6.7.2",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.100.0"
  }
}
